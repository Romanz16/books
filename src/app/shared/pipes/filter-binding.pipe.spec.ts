import { FilterBindingPipe } from './filter-binding.pipe';

describe('FilterBindingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBindingPipe();
    expect(pipe).toBeTruthy();
  });
});
