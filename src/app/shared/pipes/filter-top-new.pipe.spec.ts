import { FilterTopNewPipe } from './filter-top-new.pipe';

describe('FilterTopNewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTopNewPipe();
    expect(pipe).toBeTruthy();
  });
});
