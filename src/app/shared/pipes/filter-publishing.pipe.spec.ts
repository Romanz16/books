import { FilterPublishingPipe } from './filter-publishing.pipe';

describe('FilterPublishingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPublishingPipe();
    expect(pipe).toBeTruthy();
  });
});
