import { FilterDiscountsPipe } from './filter-discounts.pipe';

describe('FilterDiscountsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDiscountsPipe();
    expect(pipe).toBeTruthy();
  });
});
